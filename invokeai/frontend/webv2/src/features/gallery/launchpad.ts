/**
 * Gallery surface for the Launchpad. Exported eagerly — see the note in the
 * Launchpad's `livePanels` module about the single shared lazy boundary.
 */
export { RecentOutputs } from './ui/launchpad/RecentOutputs';
