import { Text } from '@chakra-ui/react';

export const LayoutActionsWidgetView = () => (
  <Text color="fg.subtle" fontSize="2xs">
    Reset restores the active preset. Recover reopens the shell regions.
  </Text>
);
