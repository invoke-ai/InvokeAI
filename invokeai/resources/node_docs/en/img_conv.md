# Convert Image Mode

Converts an image to a different mode (for example, `L`, `RGB`, `RGBA`, etc.). Use this node when a downstream operation requires a specific image mode or to change color/alpha representation.

## Inputs

- `image`: The image to convert.
- `mode`: The mode to convert the image to (e.g., `L`, `RGB`, `RGBA`). Default is `L`.
